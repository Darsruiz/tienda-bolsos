import { MostrarColorPipe } from './mostrar-color.pipe';

describe('MostrarColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarColorPipe();
    expect(pipe).toBeTruthy();
  });
});
