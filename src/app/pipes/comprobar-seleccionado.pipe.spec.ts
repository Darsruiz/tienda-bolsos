import { ComprobarSeleccionadoPipe } from './comprobar-seleccionado.pipe';

describe('ComprobarSeleccionadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ComprobarSeleccionadoPipe();
    expect(pipe).toBeTruthy();
  });
});
